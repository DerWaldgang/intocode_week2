let languages = ['js', 'python', 'c', 'c++', 'c#','java', 'zig','go','kotlin']

let symbols = 3
let count = 0
while (count < languages.length){
    if(languages[count].length > symbols){
        console.log(languages[count])
    }
    count ++

} 